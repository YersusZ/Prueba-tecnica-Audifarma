version: "3.9"

services:
  audifarma:
    build:
      context: .
      dockerfile: Dockerfile
    image: audifarma:local
    container_name: audifarma
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: default
    restart: unless-stopped
